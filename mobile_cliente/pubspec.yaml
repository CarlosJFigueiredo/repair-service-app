name: mobile_cliente
description: Tecnico Resolve - App do Cliente

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # HTTP client
  dio: ^5.4.3

  # Armazenamento local (JWT token)
  shared_preferences: ^2.2.3

  # Gerenciamento de estado
  provider: ^6.1.2

  # Ícones extras
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
